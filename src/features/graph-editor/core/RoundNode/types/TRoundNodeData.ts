// eslint-disable-next-line @typescript-eslint/consistent-type-definitions
type TRoundNodeData = {
  label: string;
  isStart: boolean;
  isCurrent: boolean;
  isInQueue: boolean;
  isVisited: boolean;
};

export type { TRoundNodeData };
